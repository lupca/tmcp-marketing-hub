const { chromium } = require('playwright');
(async () => {
    // we can't easily reproduce the whole e2e setup here without the test framework, let's just run playwright test with an environment variable for debugging
})();
